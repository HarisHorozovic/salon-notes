import {StyleSheet} from 'react-native';

export const notePreviewStyle = StyleSheet.create({
  button_container: {
    alignItems: 'flex-end',
    flexDirection: 'row-reverse',
  },
  title: {
    fontWeight: 'bold',
  },
});
